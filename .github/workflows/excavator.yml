name: Excavator

on:
  push:
    paths:
      - ".github/**/*.yml"
      - "bucket/*.json"
    branches:
      - main

jobs:
  excavate:
    name: Excavate
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@main
      - name: Excavate
        run: |
          python bin/generate.py
      - name: Check Modified Files
        id: modified_files
        uses: ./.github/actions/check-modified-files
        with:
          glob_pattern: "bucket/*.json"
      - name: Excavate
        if: steps.modified_files.outputs.files_changed == 'true'
        run: |
          for file in ${{ steps.modified_files.outputs.modified_files }}
          do
            python bin/generate.py "$file"
          done
